export enum Transitions{
    PARENT = "PARENT",
    NEXT = "NEXT",
    PREVIOUS = "PREVIOUS",
    FIRST = "FIRST",
    LAST = "LAST"
};
export enum SelectorTypes{
    CSS = "CSS",
    XPATH = "XPATH"
}