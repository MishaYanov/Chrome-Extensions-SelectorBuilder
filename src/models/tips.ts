export const tips = {
    querySelector: "querySelector()/evaluate is a method that returns the first element that matches a specified CSS selector(s) in the document.",
    querySelectorAll: "querySelectorAll()/evaluate is a method that returns a static (not live) NodeList representing a list of the document's elements that match the specified group of selectors.(evaluate can be used with itirator only)",
    has: ":has()/[tag] is a method that returns a boolean indicating whether the element would be selected by the specified selector string.",
    not: ":not() is a method that return object with elements that do not match the given selector.",
    include: "include/contains is a method that returns an object with elements that match the given selector.",
    loop: "nth-of-type/position is a method that returns an object with elements that match the given selector with starting position and the interval of the loop.",
}